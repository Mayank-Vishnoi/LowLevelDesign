public class InternalDispatcher {
    // this should ideally handle only this car right? why need a list here?

    private List<ElevatorController> elevatorControllerList;
    
    public void submitInternalRequest(String elevatorId) {
        // it needs which cart to send but only given floor?
    }
}
